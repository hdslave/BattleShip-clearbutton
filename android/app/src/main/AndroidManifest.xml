<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — Phase 2 scaffold.

    Phase 3 will swap MainActivity for a SDLActivity subclass and add
    the GLES + audio plumbing. For now, MainActivity is a no-op text
    placeholder that confirms the APK installs and libmain.so loads.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for OpenGL ES 3.0 surface (Phase 3 SDLActivity uses GLES3). -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <application
        android:label="@string/app_name"
        android:hasCode="true"

        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"

        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|screenLayout|smallestScreenSize|uiMode|navigation"
            android:launchMode="singleInstance"
            android:screenOrientation="sensorLandscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
